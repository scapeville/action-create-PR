name: Create PR
description: foo
author: Nicholas Valentinus
branding:
  icon: tool
  color: orange

inputs:
  foo:
    description: 
    required: false

runs:
  using: 'composite'
  steps:
    - run: |
        random_hex=$(openssl rand -hex 3)
        echo "Random Hex: $random_hex"
        # git checkout -b action-create-PR
      shell: bash